name: Update Smiirl Counter

on:
  workflow_dispatch: # Allows manual trigger from the Actions tab

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      # Step 3: Install Node.js dependencies
      - name: Install Node.js dependencies
        run: npm install

      # Step 4: Run the Puppeteer script
      - name: Run Puppeteer script
        env:
          GITHUB_TOKEN: ${{ secrets.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 }} # Use the new Personal Access Token
        run: npm start
